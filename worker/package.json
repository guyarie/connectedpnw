{
  "name": "connectedpnw-content-service",
  "private": true,
  "type": "module",
  "description": "MCP + REST editing service for connectedpnw.com (Cloudflare Worker)",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "secret": "wrangler secret put",
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run",
    "check": "npm run typecheck && npm run test"
  },
  "dependencies": {
    "@cloudflare/workers-oauth-provider": "0.10.3",
    "yaml": "^2.9.1",
    "zod-to-json-schema": "^3.25.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260918.1",
    "typescript": "^5.6.0",
    "vitest": "^5.0.1",
    "wrangler": "^4.0.0"
  }
}
